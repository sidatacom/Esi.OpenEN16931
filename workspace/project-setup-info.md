# Project Setup Info - Basic Vite Template

This is a minimal project setup using Vite with TypeScript. The files created are:

- `package.json`: Vite + TS dependencies
- `tsconfig.json`: TypeScript configuration
- `README.md`: Project documentation

Use this as the foundation for new projects that need clean initialization without inheriting existing codebases.